import 'package:flutter/material.dart';

class Advertiser extends StatelessWidget {
  const Advertiser({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Advertiser'));
  }
}
